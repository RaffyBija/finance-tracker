import BalanceOutlookCard from '../BalanceOutlookCard';

// Widget "Andamento del saldo" — riusa as-is la card proiezione/stima esistente.
export default function ProjectionWidget() {
  return <BalanceOutlookCard />;
}
